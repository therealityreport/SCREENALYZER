track:
  track_buffer: 10  # Restore longer buffer (re-ID will handle legitimate gaps)
  match_thresh: 0.9  # Stricter appearance matching (was 0.8)
  conf_thresh: 0.5
  iou_thresh: 0.3   # Stricter spatial matching (was 0.5)
