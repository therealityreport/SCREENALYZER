# Phase 1 QA Report — 2025-10-29

| Item | Status | Evidence & Notes |
| --- | --- | --- |
| 1. Output parity across harvest + analytics artifacts | **Pass** | `accuracy_report.json` and `delta_table.csv` list the same per-identity totals that appear in `totals.csv` (e.g., KIM 48 751 ms auto → 48.8 s) and the same pass/fail deltas; `timeline.csv` holds 45 rows for the seven labeled identities and no `SKIP`; `merge_suggestions.parquet` exists under `data/harvest/.../assist/` (0 rows, schema present) confirming the assist directory is populated. |
| 2. Per-identity enforcement wired in code | **Pass** | `screentime/tracking/reid.py:149-165` applies per-identity `freeze`, `min_sim`, and `min_margin` overrides on top of the global defaults declared in the constructor span (`screentime/tracking/reid.py:60-67`); `screentime/attribution/timeline.py:191-197` pulls per-identity `gap_merge_ms_max`/`min_interval_quality`, and the edge epsilon guard is live at `screentime/attribution/timeline.py:215-224` with the constructor default set to 150 ms (`screentime/attribution/timeline.py:56-62`). |
| 3. Bootstrap labels usage + SKIP handling | **Fail** | No worker logs mention `bootstrap_labels`, and `rg -n "bootstrap_labels_task" -g '*.py'` only returns the definition (no callers). `clusters.json` lacks the `label_source` flag that `bootstrap_labels.py:204-210` would stamp when assigning names, so current labels appear to be manual. SKIP exclusion is confirmed: cluster `4` remains unnamed in `clusters.json`, and analytics export totals cover seven people with `people_detected: 7` (`data/harvest/.../diagnostics/reports/analytics_stats.json`). |
| 4. Discrepancy log / doc drift | **Pass (documented)** | `validation_summary.txt` still reports missing merge suggestions even though the parquet now exists (empty schema). `docs/REVIEWS/2025-10-29/QA_REPORT.md` and `CHANGESET_SUMMARY.md` reference earlier failures (e.g., absent merge suggestions) that no longer apply. The generated `merge_suggestions.parquet` schema (`from_cluster_id`, `to_cluster_id`, `similarity`, `reason`) diverges from what `jobs/tasks/cluster.py:172-180` and `app/labeler.py:726-768` expect (`cluster_a_id`, `cluster_b_id`, …), so UI code may mis-read future non-empty files. |
